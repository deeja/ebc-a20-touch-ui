"""Version/build metadata for the About dialog - overwritten by
deploy/write_build_info.py before packaging a release (see
.github/workflows/release.yml and deploy/build-package.sh); these are the
plain source-checkout defaults."""
VERSION = "0.0"
BUILD_DATE = "0000"
